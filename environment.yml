name: python_example   # change the name here 
channels:
    - conda-forge
    - defaults
dependencies:           # include all the packages you need in the project
    - python>=3.9
    - pytest
    - sphinx
    - pip
    - matplotlib
    - pip:              # include the packages that can only be installed by pip
        - myst_parser
        - nbsphinx
        - mdutils
